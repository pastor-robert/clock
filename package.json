{
  "name": "clock",
  "version": "1.0.0",
  "description": "A minimalist digital clock for your browser",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint *.js && stylelint *.css",
    "lint:js": "eslint *.js",
    "lint:css": "stylelint *.css",
    "validate": "html-validate *.html",
    "test": "npm run lint && npm run validate"
  },
  "devDependencies": {
    "eslint": "^10.0.2",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^40.0.0",
    "html-validate": "^10.9.0"
  }
}
